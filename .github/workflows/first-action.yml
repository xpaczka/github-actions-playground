name: First Workflow

on: workflow_dispatch # triggered manually by the developer

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: First step
        run: echo "Hello world"

      - name: Second step
        run: echo "Goodbye"
